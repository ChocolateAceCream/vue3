import _ from 'lodash'

const state = () => ({
})

const getters = {
  // doubleCount (state) {
  //   return state.count * 2
  // }
}

const mutations = {
  // removeDesignablePId(state, pid) {
  //   const i = _.findIndex(state.designablePIds, pid)
  //   state.designablePIds.splice(i, 1)
  // }
}

const actions = {
  // incrementIfOddOnRootSum ({ state, commit, rootState }) {
  //   if ((state.count + rootState.count) % 2 === 1) {
  //     commit('increment')
  //   }
  // }
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
